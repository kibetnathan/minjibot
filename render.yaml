# Render blueprint for deploying the MinjiBot bot + database.
#
# Services
#   - minjibot-db  : managed Postgres
#   - minjibot-bot : background worker running the Discord bot (gateway).
#                    Migrations run automatically on deploy (preDeployCommand).
#
# The bot connects outbound to Discord over the gateway, so it is a
# background worker (no inbound web traffic required).

databases:
  - name: minjibot-db
    databaseName: minjibot
    user: minjibot
    plan: free
    ipAllowList: [] # only allow internal connections

services:
  - type: worker
    name: minjibot-bot
    runtime: docker
    repo: https://github.com/kibetnathan/minjibot
    branch: develop
    dockerfilePath: ./Dockerfile
    plan: free
    # Run database migrations before the bot container starts (on every deploy).
    preDeployCommand: ./scripts/migrate.sh
    envVars:
      - key: DB_URL
        fromDatabase:
          name: minjibot-db
          property: connectionString
      - key: GOOSE_DBSTRING
        fromDatabase:
          name: minjibot-db
          property: connectionString
      - key: GOOSE_DRIVER
        value: postgres
      # --- Discord ---
      - key: DISCORD_TOKEN
        sync: false
      - key: DISCORD_CLIENT_ID
        sync: false
      - key: DISCORD_CLIENT_SECRET
        sync: false
      # --- Optional integrations (leave blank if unused) ---
      - key: GOOGLE_FACTCHECK_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        sync: false
